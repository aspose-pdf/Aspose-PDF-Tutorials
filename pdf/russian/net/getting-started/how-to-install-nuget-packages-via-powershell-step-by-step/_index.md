---
category: general
date: 2026-02-20
description: Узнайте, как устанавливать пакеты NuGet с помощью PowerShell, запускать
  PowerShell от имени администратора, выводить список установленных пакетов и проверять
  установленный пакет за несколько минут.
draft: false
keywords:
- how to install nuget
- run powershell as admin
- list installed packages
- how to verify package
- verify installed package
language: ru
og_description: как установить пакеты NuGet с помощью PowerShell, запустить PowerShell
  от имени администратора, вывести список установленных пакетов и проверить установленный
  пакет — полный пошаговый гид.
og_title: Как установить пакеты NuGet через PowerShell — краткое руководство
tags:
- PowerShell
- NuGet
- Package Management
title: Как установить пакеты NuGet через PowerShell — пошагово
url: /ru/net/getting-started/how-to-install-nuget-packages-via-powershell-step-by-step/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# как установить пакеты nuget через PowerShell – пошагово

Когда‑нибудь задавались вопросом **how to install nuget** пакетов без открытия Visual Studio? Вы не одиноки. Во многих CI‑конвейерах или на новых машинах самый быстрый путь — перейти в PowerShell — желательно *run powershell as admin*—и позволить менеджеру пакетов выполнить свою работу.

В этом руководстве мы пройдем весь процесс: откроем нужную консоль, загрузим конкретную версию библиотеки и, наконец, подтвердим, что пакет действительно установился в вашей системе. К концу вы сможете **list installed packages**, знать **how to verify package** целостность и быть уверенными, что шаг **verify installed package** выполнен каждый раз.

## Что вы узнаете

- Как запустить PowerShell с правильными привилегиями.  
- Точный синтаксис команды `Install-Package` для NuGet.  
- Способы **list installed packages** и подтверждение номеров версий.  
- Распространённые подводные камни (отсутствие прав администратора, несоответствие версий) и как их избежать.  

Предыдущий опыт работы с NuGet не требуется, достаточно работающей Windows‑машины и небольшого любопытства.

---

## Как установить пакеты NuGet с помощью PowerShell

> **Pro tip:** Если вы часто добавляете одни и те же пакеты, рассмотрите возможность добавить их в файл скрипта и запускать его с параметром `-File`. Это избавит вас от повторного ввода одной и той же строки.

### Шаг 1: Откройте PowerShell с необходимыми правами

Первое, что вам нужно сделать, — **run powershell as admin**. Без повышенных прав cmdlet `Install-Package` может тихо завершиться с ошибкой или запросить подтверждение, с которым вы не хотите разбираться.

1. Нажмите кнопку «Пуск».  
2. Введите **PowerShell**.  
3. Щелкните правой кнопкой *Windows PowerShell* и выберите **Run as administrator**.  

Вы увидите запрос UAC; нажмите **Yes**. Теперь у вас привилегированная сессия, готовая к установке пакетов.

> *Почему администратор?*  
> NuGet записывает файлы в глобальную папку пакетов (`C:\Program Files\PackageManagement\NuGet\Packages` по умолчанию). Это место защищено, поэтому только процесс с повышенными правами может туда писать.

### Шаг 2: Установите нужный пакет NuGet и его версию

With the console open, the core command is straightforward:

```powershell
# Install the Aspose.PDF library, version 25.3
Install-Package Aspose.PDF -Version 25.3
```

- `Install-Package` — это обёртка PowerShell вокруг клиента NuGet.  
- `-Version` фиксирует точную сборку, которая вам нужна, предотвращая случайные обновления.  

Если опустить `-Version`, PowerShell загрузит последнюю стабильную версию — иногда это приемлемо, иногда вам нужна точная версия, с которой вы тестировали.

#### Что происходит под капотом?

PowerShell обращается к настроенному источнику пакетов (по умолчанию `https://www.nuget.org/api/v2`) и загружает файл `.nupkg`. Затем он извлекает DLL‑файлы в глобальную папку пакетов и регистрирует пакет у локального провайдера пакетов. Весь процесс обычно завершается за несколько секунд, если только у вас нет медленного соединения.

### Шаг 3: Проверьте, что пакет успешно установлен

Теперь, когда пакет находится на диске, вы, вероятно, спросите, **«How do I verify the package?»** Ответ кроется в простом запросе:

```powershell
# List all installed NuGet packages
Get-Package -Name Aspose.PDF
```

Выполнение этого возвращает что‑то вроде:

```
Name        Version   Source
----        -------   ------
Aspose.PDF  25.3      nuget.org
```

Этот вывод подтверждает две вещи:

1. Пакет **Aspose.PDF** присутствует.  
2. Его версия соответствует запрошенной, удовлетворяя требование **verify installed package**.

Если хотите увидеть *каждый* пакет на машине, уберите фильтр `-Name`:

```powershell
Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}
```

Этот вид **list installed packages** удобен для аудитов или когда нужно очистить старые библиотеки.

### Шаг 4: Необязательно — обработка граничных случаев

#### a) Пакет не найден или версия не совпадает

Если PowerShell отвечает *«Package not found»* или *«Version not available»*, дважды проверьте написание и номер версии. NuGet нечувствителен к регистру, но лишний пробел нарушит команду.

```powershell
# Search the NuGet feed for available versions
Find-Package Aspose.PDF -AllVersions
```

#### b) Запуск без прав администратора

Если вы забудете **run powershell as admin**, cmdlet выдаст ошибку доступа. Решение простое: закройте окно и откройте его снова с повышенными правами — переустанавливать ничего не нужно.

#### c) Использование пользовательского источника

В корпоративных средах у вас может быть внутренний NuGet‑фид:

```powershell
Install-Package MyCompany.Logging -Source https://nuget.mycompany.local/api/v2
```

Шаг проверки остаётся тем же; просто не забудьте добавить `-Source` при установке.

---

## Таблица быстрого справочника

| Action                              | PowerShell command                                          | Why it matters |
|-------------------------------------|-------------------------------------------------------------|----------------|
| Открыть консоль с повышенными правами               | *Run PowerShell as Administrator*                           | Needed for global install |
| Установить конкретную версию          | `Install-Package <pkg> -Version <x.y.z>`                    | Guarantees reproducible builds |
| Показать один пакет               | `Get-Package -Name <pkg>`                                    | Confirms **how to verify package** |
| Показать все пакеты NuGet             | `Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}`| Useful for **list installed packages** |
| Поиск доступных версий           | `Find-Package <pkg> -AllVersions`                           | Helps when version is unknown |

## Заключение

Мы рассмотрели **how to install nuget** пакеты с помощью PowerShell от начала до конца — открытие консоли **run powershell as admin**, загрузка конкретной версии и, наконец, **list installed packages** для **verify installed package**. Имея эти команды в своем арсенале, вы можете автоматизировать управление библиотеками на любой Windows‑машине, будь то скрипт для CI‑конвейера или просто исправление отсутствующей DLL на вашем рабочем компьютере.

Следующие шаги? Попробуйте добавить несколько пакетов в один скрипт, изучить параметр `-Scope` для локальной установки в проекте или объединить эти команды с `Invoke-Expression`, чтобы создать лёгкий установщик для вашей команды. И если возникнет проблема, вспомните шаг **how to verify package** — просмотр версии в `Get-Package` часто самый быстрый способ обнаружить проблему.

Удачной работы с PowerShell! 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}