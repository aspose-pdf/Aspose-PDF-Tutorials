---
category: general
date: 2026-02-20
description: PowerShell का उपयोग करके NuGet पैकेज कैसे इंस्टॉल करें, PowerShell को
  एडमिन के रूप में चलाएँ, इंस्टॉल किए गए पैकेजों की सूची बनाएँ और मिनटों में इंस्टॉल
  किए गए पैकेज की पुष्टि करें।
draft: false
keywords:
- how to install nuget
- run powershell as admin
- list installed packages
- how to verify package
- verify installed package
language: hi
og_description: PowerShell का उपयोग करके NuGet पैकेज कैसे इंस्टॉल करें, PowerShell
  को एडमिन के रूप में चलाएँ, इंस्टॉल किए गए पैकेजों की सूची बनाएँ और इंस्टॉल किए गए
  पैकेज को सत्यापित करें—पूर्ण मार्गदर्शन।
og_title: PowerShell के माध्यम से NuGet पैकेज कैसे इंस्टॉल करें – त्वरित गाइड
tags:
- PowerShell
- NuGet
- Package Management
title: PowerShell के माध्यम से NuGet पैकेज कैसे इंस्टॉल करें – चरण दर चरण
url: /hi/net/getting-started/how-to-install-nuget-packages-via-powershell-step-by-step/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# PowerShell के माध्यम से NuGet पैकेज कैसे इंस्टॉल करें – चरण दर चरण

क्या आपने कभी सोचा है **how to install nuget** पैकेज Visual Studio खोले बिना? आप अकेले नहीं हैं। कई CI पाइपलाइन या नई मशीनों पर सबसे तेज़ तरीका है PowerShell खोलना—बेहतर होगा *run powershell as admin*—और पैकेज मैनेजर को अपना काम करने देना।

इस ट्यूटोरियल में हम पूरी प्रक्रिया को समझेंगे: सही कंसोल खोलना, लाइब्रेरी का एक विशिष्ट संस्करण डाउनलोड करना, और अंत में यह पुष्टि करना कि पैकेज वास्तव में आपके सिस्टम पर स्थापित हो गया है। अंत तक आप **list installed packages** कर पाएँगे, **how to verify package** की अखंडता जानेंगे, और हर बार **verify installed package** चरण की सफलता पर भरोसा महसूस करेंगे।

## आप क्या सीखेंगे

- PowerShell को सही विशेषाधिकारों के साथ लॉन्च करने का तरीका।  
- NuGet के लिए सटीक `Install-Package` कमांड सिंटैक्स।  
- **list installed packages** करने और संस्करण संख्या की पुष्टि करने के तरीके।  
- सामान्य समस्याएँ (एडमिन अधिकारों की कमी, संस्करण असंगतियाँ) और उन्हें कैसे टालें।  

NuGet के साथ कोई पूर्व अनुभव आवश्यक नहीं है, बस एक कार्यशील Windows मशीन और थोड़ी जिज्ञासा।

---

## PowerShell का उपयोग करके NuGet पैकेज कैसे इंस्टॉल करें

> **Pro tip:** यदि आप अक्सर वही पैकेज जोड़ते हैं, तो उन्हें एक स्क्रिप्ट फ़ाइल में जोड़ने और `-File` के साथ चलाने पर विचार करें। यह आपको वही लाइन बार‑बार टाइप करने से बचाता है।

### Step 1: आवश्यक अनुमतियों के साथ PowerShell खोलें

सबसे पहला काम है **run powershell as admin**। बिना एलेवेटेड अधिकारों के `Install-Package` cmdlet चुपचाप विफल हो सकता है या ऐसी पुष्टि माँग सकता है जिसे आप नहीं चाहते।

1. स्टार्ट बटन पर क्लिक करें।  
2. **PowerShell** टाइप करें।  
3. *Windows PowerShell* पर राइट‑क्लिक करें और **Run as administrator** चुनें।  

आपको एक UAC प्रॉम्प्ट दिखेगा; **Yes** पर क्लिक करें। अब आपके पास पैकेज इंस्टॉल करने के लिए एक विशेषाधिकारयुक्त सत्र तैयार है।

> *Why admin?*  
> NuGet डिफ़ॉल्ट रूप से ग्लोबल पैकेज फ़ोल्डर (`C:\Program Files\PackageManagement\NuGet\Packages`) में फ़ाइलें लिखता है। यह स्थान संरक्षित है, इसलिए केवल एलेवेटेड प्रोसेस ही वहाँ लिख सकता है।

### Step 2: इच्छित NuGet पैकेज और संस्करण इंस्टॉल करें

कंसोल खुलने पर मुख्य कमांड सीधा है:

```powershell
# Install the Aspose.PDF library, version 25.3
Install-Package Aspose.PDF -Version 25.3
```

- `Install-Package` NuGet क्लाइंट के चारों ओर PowerShell रैपर है।  
- `-Version` वह सटीक बिल्ड पिन करता है जिसकी आपको आवश्यकता है, अनजाने अपग्रेड को रोकता है।  

यदि आप `-Version` छोड़ देते हैं, तो PowerShell नवीनतम स्थिर रिलीज़ खींचेगा—कभी‑कभी यह ठीक है, कभी‑कभी आपको वही संस्करण चाहिए जो आपने परीक्षण किया था।

#### क्या होता है बैकएंड में?

PowerShell कॉन्फ़िगर किए गए पैकेज स्रोत (डिफ़ॉल्ट `https://www.nuget.org/api/v2`) से संपर्क करता है और `.nupkg` फ़ाइल डाउनलोड करता है। फिर यह DLLs को ग्लोबल पैकेज फ़ोल्डर में एक्सट्रैक्ट करता है और स्थानीय पैकेज प्रोवाइडर के साथ पैकेज को रजिस्टर करता है। पूरा प्रोसेस आमतौर पर कुछ सेकंड में समाप्त हो जाता है, जब तक आप धीमे नेटवर्क पर न हों।

### Step 3: पुष्टि करें कि पैकेज सफलतापूर्वक इंस्टॉल हुआ

अब पैकेज डिस्क पर है, आप संभवतः पूछेंगे, **“पैकेज कैसे सत्यापित करें?”** उत्तर एक सरल क्वेरी में है:

```powershell
# List all installed NuGet packages
Get-Package -Name Aspose.PDF
```

इसे चलाने पर आपको कुछ इस तरह का आउटपुट मिलेगा:

```
Name        Version   Source
----        -------   ------
Aspose.PDF  25.3      nuget.org
```

यह आउटपुट दो बातें पुष्टि करता है:

1. पैकेज **Aspose.PDF** मौजूद है।  
2. उसका संस्करण वही है जो आपने माँगा था, जिससे **verify installed package** की आवश्यकता पूरी होती है।

यदि आप मशीन पर *हर* पैकेज देखना चाहते हैं, तो `-Name` फ़िल्टर को हटा दें:

```powershell
Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}
```

यह **list installed packages** दृश्य ऑडिट या पुराने लाइब्रेरीज़ को साफ़ करने के समय बहुत उपयोगी है।

### Step 4: वैकल्पिक – एज केस संभालना

#### a) पैकेज नहीं मिला या संस्करण असंगत

यदि PowerShell *“Package not found”* या *“Version not available”* उत्तर देता है, तो स्पेलिंग और संस्करण संख्या दोबारा जांचें। NuGet केस‑इन्सेंसिटिव है, लेकिन एक अतिरिक्त स्पेस कमांड को तोड़ देगा।

```powershell
# Search the NuGet feed for available versions
Find-Package Aspose.PDF -AllVersions
```

#### b) बिना एडमिन अधिकारों के चलाना

यदि आप **run powershell as admin** करना भूल जाते हैं, तो cmdlet अनुमति त्रुटि फेंकेगा। समाधान बस विंडो को बंद करके एलेवेटेड अधिकारों के साथ फिर से खोलना है—किसी भी चीज़ को पुनः इंस्टॉल करने की जरूरत नहीं।

#### c) कस्टम स्रोत का उपयोग

कॉर्पोरेट वातावरण में आपके पास एक आंतरिक NuGet फ़ीड हो सकता है:

```powershell
Install-Package MyCompany.Logging -Source https://nuget.mycompany.local/api/v2
```

सत्यापन चरण वही रहता है; बस इंस्टॉल करते समय `-Source` शामिल करना याद रखें।

---

## Quick reference table

| क्रिया | PowerShell कमांड | क्यों महत्वपूर्ण है |
|--------|-------------------|-------------------|
| उच्चाधिकार वाला कंसोल खोलें | *Run PowerShell as Administrator* | ग्लोबल इंस्टॉल के लिए आवश्यक |
| एक विशिष्ट संस्करण स्थापित करें | `Install-Package <pkg> -Version <x.y.z>` | पुनरुत्पादनीय बिल्ड सुनिश्चित करता है |
| एकल पैकेज सूचीबद्ध करें | `Get-Package -Name <pkg>` | **पैकेज कैसे सत्यापित करें** की पुष्टि करता है |
| सभी NuGet पैकेज सूचीबद्ध करें | `Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}` | **स्थापित पैकेज सूचीबद्ध करें** के लिए उपयोगी |
| उपलब्ध संस्करण खोजें | `Find-Package <pkg> -AllVersions` | जब संस्करण अज्ञात हो तो मदद करता है |

---

## निष्कर्ष

हमने **how to install nuget** पैकेज को PowerShell के माध्यम से शुरू से अंत तक कवर किया—कंसोल **run powershell as admin** खोलना, एक विशिष्ट संस्करण डाउनलोड करना, और अंत में **list installed packages** करके **verify installed package** की पुष्टि करना। इन कमांड्स को अपने टूलबॉक्स में रखने से आप किसी भी Windows मशीन पर लाइब्रेरी प्रबंधन को स्वचालित कर सकते हैं, चाहे आप CI पाइपलाइन स्क्रिप्ट कर रहे हों या अपने डेव बॉक्स पर कोई गायब DLL ठीक कर रहे हों।

अगले कदम? कई पैकेज को एक ही स्क्रिप्ट में जोड़ने की कोशिश करें, `-Scope` पैरामीटर को एक्सप्लोर करें ताकि प्रोजेक्ट के लिए स्थानीय रूप से इंस्टॉल हो, या इन कमांड्स को `Invoke-Expression` के साथ मिलाकर अपनी टीम के लिए एक हल्का इंस्टॉलर बनाएं। और यदि कोई समस्या आती है, तो **पैकेज कैसे सत्यापित करें** चरण को याद रखें—`Get-Package` में संस्करण देखना अक्सर समस्या को जल्दी पहचानने का सबसे तेज़ तरीका होता है।

PowerShell का आनंद लें! 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}