---
category: general
date: 2026-02-20
description: เรียนรู้วิธีติดตั้งแพ็กเกจ NuGet ด้วย PowerShell, รัน PowerShell ในฐานะผู้ดูแลระบบ,
  แสดงรายการแพ็กเกจที่ติดตั้งและตรวจสอบแพ็กเกจที่ติดตั้งในเวลาไม่กี่นาที.
draft: false
keywords:
- how to install nuget
- run powershell as admin
- list installed packages
- how to verify package
- verify installed package
language: th
og_description: วิธีติดตั้งแพคเกจ NuGet ด้วย PowerShell, รัน PowerShell ในฐานะผู้ดูแลระบบ,
  แสดงรายการแพคเกจที่ติดตั้งและตรวจสอบแพคเกจที่ติดตั้ง—ขั้นตอนครบถ้วน
og_title: วิธีติดตั้งแพ็กเกจ NuGet ผ่าน PowerShell – คู่มือสั้น
tags:
- PowerShell
- NuGet
- Package Management
title: วิธีติดตั้งแพ็กเกจ NuGet ผ่าน PowerShell – ทีละขั้นตอน
url: /th/net/getting-started/how-to-install-nuget-packages-via-powershell-step-by-step/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีการติดตั้ง nuget packages ผ่าน PowerShell – ทีละขั้นตอน

เคยสงสัย **how to install nuget** packages โดยไม่ต้องเปิด Visual Studio หรือไม่? คุณไม่ได้เป็นคนเดียว ในหลาย ๆ pipeline ของ CI หรือบนเครื่องใหม่ ๆ วิธีที่เร็วที่สุดคือการเปิด PowerShell—โดยควร *run powershell as admin*—และให้ผู้จัดการแพ็กเกจทำงานของมัน

ในบทแนะนำนี้ เราจะเดินผ่านกระบวนการทั้งหมด: การเปิดคอนโซลที่ถูกต้อง, ดึงเวอร์ชันเฉพาะของไลบรารี, และสุดท้ายยืนยันว่าแพ็กเกจได้ถูกติดตั้งบนระบบของคุณจริง ๆ เมื่อจบคุณจะสามารถ **list installed packages**, รู้ **how to verify package** integrity, และมั่นใจว่าขั้นตอน **verify installed package** สำเร็จทุกครั้ง

## สิ่งที่คุณจะได้เรียนรู้

- วิธีเปิด PowerShell ด้วยสิทธิ์ที่ถูกต้อง.  
- ไวยากรณ์คำสั่ง `Install-Package` ที่แม่นยำสำหรับ NuGet.  
- วิธี **list installed packages** และยืนยันหมายเลขเวอร์ชัน.  
- ข้อผิดพลาดทั่วไป (ขาดสิทธิ์ผู้ดูแล, ความไม่ตรงกันของเวอร์ชัน) และวิธีหลีกเลี่ยง.  

ไม่จำเป็นต้องมีประสบการณ์กับ NuGet มาก่อน เพียงแค่เครื่อง Windows ที่ทำงานได้และความอยากรู้อยากเห็นเล็กน้อย.

---

## วิธีการติดตั้ง NuGet Packages Using PowerShell

> **เคล็ดลับ:** หากคุณมักเพิ่มแพ็กเกจเดียวกันบ่อย ๆ ให้พิจารณาเพิ่มลงในไฟล์สคริปต์และเรียกใช้ด้วย `-File`. มันช่วยคุณไม่ต้องพิมพ์บรรทัดเดียวกันซ้ำ ๆ

### ขั้นตอน 1: เปิด PowerShell ด้วยสิทธิ์ที่จำเป็น

สิ่งแรกที่คุณต้องทำคือ **run powershell as admin**. หากไม่มีสิทธิ์ระดับสูง คำสั่ง `Install-Package` อาจล้มเหลวโดยไม่มีข้อความหรือขอการยืนยันที่คุณไม่ต้องการจัดการ.

1. คลิกที่ปุ่ม Start.  
2. พิมพ์ **PowerShell**.  
3. คลิกขวาที่ *Windows PowerShell* แล้วเลือก **Run as administrator**.  

คุณจะเห็นหน้าต่าง UAC; คลิก **Yes**. ตอนนี้คุณมีเซสชันที่มีสิทธิ์พร้อมสำหรับการติดตั้งแพ็กเกจ.

> *ทำไมต้องเป็น admin?*  
> NuGet เขียนไฟล์ไปยังโฟลเดอร์แพ็กเกจทั่วโลก (`C:\Program Files\PackageManagement\NuGet\Packages` โดยค่าเริ่มต้น). ที่ตั้งนี้ได้รับการปกป้อง ดังนั้นจึงต้องใช้กระบวนการที่มีสิทธิ์ระดับสูงจึงจะเขียนได้.

### ขั้นตอน 2: ติดตั้งแพ็กเกจ NuGet ที่ต้องการและเวอร์ชัน

With the console open, the core command is straightforward:

```powershell
# Install the Aspose.PDF library, version 25.3
Install-Package Aspose.PDF -Version 25.3
```

- `Install-Package` คือ wrapper ของ PowerShell สำหรับคลไคลเอนต์ของ NuGet.  
- `-Version` กำหนดเวอร์ชันที่ต้องการอย่างแม่นยำ ป้องกันการอัปเกรดโดยไม่ได้ตั้งใจ.  

หากคุณละเว้น `-Version` PowerShell จะดึงเวอร์ชันเสถียรล่าสุด—บางครั้งก็โอเค, บางครั้งคุณต้องการเวอร์ชันที่ตรงกับที่คุณทดสอบ.

#### สิ่งที่เกิดขึ้นภายใน

PowerShell ติดต่อแหล่งแพ็กเกจที่กำหนด (โดยค่าเริ่มต้น `https://www.nuget.org/api/v2`) และดาวน์โหลดไฟล์ `.nupkg`. จากนั้นจะสกัด DLLs ไปยังโฟลเดอร์แพ็กเกจทั่วโลกและลงทะเบียนแพ็กเกจกับผู้ให้บริการแพ็กเกจในเครื่อง. กระบวนการทั้งหมดมักเสร็จในไม่กี่วินาที ยกเว้นคุณอยู่บนเครือข่ายช้า.

### ขั้นตอน 3: ยืนยันว่าแพ็กเกจถูกติดตั้งสำเร็จ

ตอนนี้แพ็กเกจอยู่บนดิสก์แล้ว คุณอาจถามว่า **“How do I verify the package?”** คำตอบอยู่ในคำสั่งง่าย ๆ:

```powershell
# List all installed NuGet packages
Get-Package -Name Aspose.PDF
```

การรันนี้จะคืนค่าแบบนี้:

```
Name        Version   Source
----        -------   ------
Aspose.PDF  25.3      nuget.org
```

ผลลัพธ์นี้ยืนยันสองเรื่อง:

1. แพ็กเกจ **Aspose.PDF** มีอยู่.  
2. เวอร์ชันตรงกับที่คุณระบุ, ตรงตามความต้องการของ **verify installed package**.  

หากคุณต้องการดู *ทุก* แพ็กเกจบนเครื่อง ให้ลบตัวกรอง `-Name`:

```powershell
Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}
```

มุมมอง **list installed packages** นี้มีประโยชน์สำหรับการตรวจสอบหรือเมื่อคุณต้องทำความสะอาดไลบรารีเก่า.

### ขั้นตอน 4: ตัวเลือก – การจัดการกรณีขอบ

#### a) ไม่พบแพ็กเกจหรือเวอร์ชันไม่ตรงกัน

หาก PowerShell ตอบว่า *“Package not found”* หรือ *“Version not available”*, ตรวจสอบการสะกดและหมายเลขเวอร์ชันอีกครั้ง. NuGet ไม่สนใจตัวพิมพ์ใหญ่/เล็ก, แต่ช่องว่างที่ไม่จำเป็นจะทำให้คำสั่งล้มเหลว.

```powershell
# Search the NuGet feed for available versions
Find-Package Aspose.PDF -AllVersions
```

#### b) รันโดยไม่มีสิทธิ์ admin

หากคุณลืม **run powershell as admin**, คำสั่งจะโยนข้อผิดพลาดเรื่องสิทธิ์. วิธีแก้คือปิดหน้าต่างและเปิดใหม่ด้วยสิทธิ์ระดับสูง—ไม่ต้องติดตั้งใหม่.

#### c) ใช้แหล่งที่กำหนดเอง

ในสภาพแวดล้อมองค์กรคุณอาจมีฟีด NuGet ภายใน:

```powershell
Install-Package MyCompany.Logging -Source https://nuget.mycompany.local/api/v2
```

ขั้นตอนการตรวจสอบยังคงเหมือนเดิม; เพียงจำไว้ว่าให้ใส่ `-Source` เมื่อทำการติดตั้ง.

---

## ตารางอ้างอิงอย่างรวดเร็ว

| การกระทำ                           | PowerShell command                                          | เหตุผลที่สำคัญ |
|------------------------------------|-------------------------------------------------------------|----------------|
| เปิดคอนโซลที่มีสิทธิ์ระดับสูง    | *Run PowerShell as Administrator*                           | Needed for global install |
| ติดตั้งเวอร์ชันเฉพาะ               | `Install-Package <pkg> -Version <x.y.z>`                    | Guarantees reproducible builds |
| แสดงรายการแพ็กเกจเดียว            | `Get-Package -Name <pkg>`                                    | Confirms **how to verify package** |
| แสดงรายการแพ็กเกจ NuGet ทั้งหมด    | `Get-Package | Where-Object {$_.ProviderName -eq 'NuGet'}`| Useful for **list installed packages** |
| ค้นหาเวอร์ชันที่มีอยู่             | `Find-Package <pkg> -AllVersions`                           | Helps when version is unknown |

## สรุป

เราได้ครอบคลุม **how to install nuget** packages โดยใช้ PowerShell ตั้งแต่ต้นจนจบ—การเปิดคอนโซล **run powershell as admin**, ดึงเวอร์ชันเฉพาะ, และสุดท้าย **list installed packages** เพื่อ **verify installed package**. ด้วยคำสั่งเหล่านี้ในชุดเครื่องมือของคุณ คุณสามารถอัตโนมัติการจัดการไลบรารีบนเครื่อง Windows ใดก็ได้ ไม่ว่าจะสคริปต์ CI pipeline หรือเพียงแก้ไข DLL ที่หายไปในกล่องพัฒนาของคุณ.

ขั้นตอนต่อไป? ลองเพิ่มหลายแพ็กเกจในสคริปต์เดียว, สำรวจพารามิเตอร์ `-Scope` เพื่อติดตั้งแบบโลคัลสำหรับโปรเจกต์, หรือรวมคำสั่งเหล่านี้กับ `Invoke-Expression` เพื่อสร้างตัวติดตั้งเบา ๆ สำหรับทีมของคุณ. และหากคุณเจออุปสรรค, จำขั้นตอน **how to verify package**—การดูเวอร์ชันใน `Get-Package` มักเป็นวิธีที่เร็วที่สุดในการตรวจพบปัญหา.

ขอให้สนุกกับ PowerShell! 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}