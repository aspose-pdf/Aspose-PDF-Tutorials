---
category: general
date: 2025-12-31
description: إنشاء مستند PDF باستخدام Aspose.PDF في C#. تعلم كيفية إضافة صفحة إلى
  PDF، وإضافة مربع نص، وحفظ PDF مع نموذج في دليل واحد.
draft: false
keywords:
- create pdf document
- add page to pdf
- save pdf with form
- how to add text box
- how to create pdf form
language: ar
og_description: إنشاء مستند PDF باستخدام Aspose.PDF. يوضح هذا الدليل كيفية إضافة صفحة
  إلى PDF، وإدراج مربع نص، وحفظ PDF مع نموذج.
og_title: إنشاء مستند PDF باستخدام Aspose – إضافة صفحة، مربع نص، نموذج
tags:
- Aspose.Pdf
- C#
- PDF Forms
title: إنشاء مستند PDF باستخدام Aspose – إضافة صفحة، صندوق نص، ونموذج
url: /ar/net/forms-annotations/create-pdf-document-with-aspose-add-page-text-box-and-form/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# إنشاء مستند PDF باستخدام Aspose – إضافة صفحة، مربع نص، ونموذج

هل احتجت يومًا إلى **إنشاء مستند PDF** برمجيًا وتساءلت من أين تبدأ؟ لست وحدك—المطورون يسألون باستمرار: “كيف أضيف صفحة إلى PDF وأدمج حقل نموذج دون عناء؟” الخبر السار هو أن Aspose.PDF يجعل ذلك سهلًا للغاية. في هذا الدرس سنستعرض العملية بالكامل: من تهيئة PDF، **إضافة صفحة إلى PDF**، إدراج **مربع نص**، وأخيرًا **حفظ PDF مع نموذج** بحيث يكون جاهزًا للمستخدمين النهائيين.

سنغطي كل ما تحتاج معرفته، بما في ذلك سبب أهمية كل خطوة، الأخطاء الشائعة، وبعض النصائح الاحترافية التي ستوفر لك الوقت لاحقًا. في النهاية ستحصل على ملف PDF يعمل بالكامل يحتوي على عنصرين من مربعات النص المرتبطة—مثالي للتوقيعات، التعليقات، أو أي سيناريو جمع بيانات.

## ما ستتعلمه

- كيفية **إنشاء مستند PDF** من الصفر باستخدام Aspose.PDF for .NET.  
- الكود الدقيق **لإضافة صفحة إلى PDF** وتحديد مواضع العناصر بدقة.  
- الطريقة الصحيحة **لإضافة مربع نص** كحقل نموذج، وكيفية ربط عدة عناصر بواجهة واحدة لنفس الحقل.  
- كيفية **حفظ PDF مع نموذج** بحيث تبقى الحقول تفاعلية عند فتحها في Adobe Reader أو أي عارض PDF.  
- نصائح لتصحيح الأخطاء وتوسيع المثال (مثل إضافة التحقق، ضبط الخطوط، أو دمج صفحات متعددة).

### المتطلبات المسبقة

- .NET 6.0 أو أحدث (الكود يعمل أيضًا مع .NET Framework 4.6+).  
- حزمة Aspose.PDF for .NET عبر NuGet (`Install-Package Aspose.Pdf`).  
- فهم أساسي لصياغة C#—ليس هناك حاجة لمعرفة عميقة بـ PDF.  

إذا كان لديك هذه المتطلبات، فلنبدأ.

## إنشاء مستند PDF – تهيئة Aspose PDF

أول شيء علينا فعله هو إنشاء كائن **Document**. فكر فيه كقماش فارغ ستُرسم عليه كل العناصر الأخرى.

```csharp
using Aspose.Pdf;
using Aspose.Pdf.Forms;

// Step 1: Create a new PDF document (this is the core of create pdf document)
Document pdfDocument = new Document();
```

> **لماذا هذا مهم:** فئة `Document` تحزم ملف PDF بالكامل—البيانات الوصفية، الصفحات، التعليقات، وحقول النماذج. بدونها لا يمكنك إضافة صفحة أو عنصر لاحقًا.

## إضافة صفحة إلى PDF – إعداد القماش

ملف PDF بدون صفحات هو في الأساس ملف شبح. إضافة صفحة أمر بسيط، لكن الإحداثيات التي تختارها ستحدد مكان ظهور حقول النموذج.

```csharp
// Step 2: Add a single page to the document
Page pdfPage = pdfDocument.Pages.Add();

// Optional: set page size if you need something other than A4
// pdfPage.SetPageSize(PageSize.A4.Width, PageSize.A4.Height);
```

> **نصيحة احترافية:** يستخدم Aspose نظام إحداثيات حيث (0,0) هو الزاوية السفلية اليسرى. الـ `Rectangle` الذي سنستخدمه لاحقًا يتوقع القيم بالنقاط (1 نقطة = 1/72 بوصة). ضع ذلك في اعتبارك عند تحديد موضع العناصر.

## كيفية إضافة مربع نص – تعريف حقول النموذج

الآن يأتي الجزء الممتع: إنشاء **مربع نص** يمكن للمستخدمين ملؤه. في مصطلحات PDF يُطلق على هذا `TextBoxField`. سننشئ حقلًا واحدًا يحتوي على عنصرين بصريين—حتى تظهر القيمة نفسها في مكانين على الصفحة.

```csharp
// Step 3: Define the first text box widget (the actual field definition)
TextBoxField firstTextBox = new TextBoxField(pdfPage, new Rectangle(100, 600, 300, 650))
{
    PartialName = "tb1",          // field name – must be unique within the form
    Value = "Enter text here",    // default placeholder text
    // Optional visual tweaks:
    Border = new Border(BorderStyle.Solid, 1, Color.Black),
    BackgroundColor = Color.LightGray,
    TextAlignment = HorizontalAlignment.Center
};

// Step 4: Define a second widget for the same field (appears lower on the page)
TextBoxField secondTextBoxWidget = new TextBoxField(pdfPage, new Rectangle(100, 500, 300, 550))
{
    PartialName = "tb1"   // same name links it to the first widget
};
```

> **لماذا عنصرين؟** ربط عدة مستطيلات بنفس `PartialName` يخلق حقلًا منطقيًا *واحدًا* له تمثيلات بصرية متعددة. ما يكتبه المستخدم في أحد الصناديق يظهر فورًا في الآخر—مفيد للبيانات المتكررة مثل “معرّف العميل”.

### إضافة الحقل إلى النموذج

يتطلب Aspose تسجيل الحقل في مجموعة نماذج المستند، ثم إرفاق أي عناصر إضافية يدويًا.

```csharp
// Step 5: Register the field (the first widget is automatically added)
pdfDocument.Form.Add(firstTextBox, "tb1", 1);

// Attach the second widget to the same field
pdfPage.Annotations.Add(secondTextBoxWidget);
```

> **تحذير:** إذا نسيت استدعاء `Form.Add`، لن يكون الحقل تفاعليًا عند فتح PDF. أضف العنصر الأساسي أولًا، ثم أي عناصر إضافية.

## حفظ PDF مع نموذج – إكمال المستند

لقد بنينا الهيكل؛ الآن نكتب الملف إلى القرص. طريقة `Save` تكتب الملف مع الحفاظ على جميع العناصر التفاعلية.

```csharp
// Step 6: Save the PDF – the file will contain both text box widgets
string outputPath = @"C:\Temp\TextBoxWithTwoWidgets.pdf";
pdfDocument.Save(outputPath);
```

> **النتيجة:** افتح PDF الناتج في Adobe Reader. سترى مربعين نصيين متطابقين؛ الكتابة في أحدهما تُحدّث الآخر فورًا. الملف جاهز تمامًا لـ **save pdf with form** ويمكن توزيعه على المستخدمين لجمع البيانات.

## مثال كامل يعمل

فيما يلي البرنامج الكامل جاهز للنسخ واللصق. يُجمّع كطبيق console، لكن يمكنك دمج المنطق نفسه في أي مشروع .NET.

```csharp
using System;
using Aspose.Pdf;
using Aspose.Pdf.Forms;
using Aspose.Pdf.Drawing;   // for Color, Border, etc.

class Program
{
    static void Main()
    {
        // 1️⃣ Create PDF document
        Document pdfDocument = new Document();

        // 2️⃣ Add a page
        Page pdfPage = pdfDocument.Pages.Add();

        // 3️⃣ First text box (primary widget)
        TextBoxField firstTextBox = new TextBoxField(pdfPage,
            new Rectangle(100, 600, 300, 650))
        {
            PartialName = "tb1",
            Value = "Enter text here",
            Border = new Border(BorderStyle.Solid, 1, Color.Black),
            BackgroundColor = Color.LightGray,
            TextAlignment = HorizontalAlignment.Center
        };

        // 4️⃣ Second widget linked to the same field
        TextBoxField secondTextBoxWidget = new TextBoxField(pdfPage,
            new Rectangle(100, 500, 300, 550))
        {
            PartialName = "tb1"
        };

        // 5️⃣ Register field and attach extra widget
        pdfDocument.Form.Add(firstTextBox, "tb1", 1);
        pdfPage.Annotations.Add(secondTextBoxWidget);

        // 6️⃣ Save the document
        string outputPath = @"C:\Temp\TextBoxWithTwoWidgets.pdf";
        pdfDocument.Save(outputPath);

        Console.WriteLine($"PDF created successfully at: {outputPath}");
    }
}
```

### النتيجة المتوقعة

- ملف باسم **TextBoxWithTwoWidgets.pdf** في المجلد المحدد.  
- مربعا نص متطابقين يحملان النص “Enter text here”.  
- تعديل أيٍ من الصناديق يُحدّث الآخر فورًا—دليل على أن الحقل مشترك فعليًا.  

افتح الـ PDF بأي عارض يدعم AcroForms (Adobe Reader، Foxit، Chrome) واختبر التفاعلية.

## أسئلة شائعة وحالات خاصة

**س: ماذا لو احتجت إلى أكثر من عنصرين؟**  
ج: فقط أنشئ مثيلات إضافية من `TextBoxField` بنفس `PartialName` وأضفها إلى `pdfPage.Annotations`. لا يوجد حد أقصى.

**س: هل يمكن تحديد الحد الأقصى لطول الأحرف؟**  
ج: نعم. عيّن `firstTextBox.MaxLength = 50;` (أو أي عدد صحيح) قبل إضافة الحقل.

**س: كيف أجعل الحقل إلزاميًا؟**  
ج: استخدم `firstTextBox.Required = true;`. معظم العارضات ستُبرز الحقل إذا تم إرسال النموذج فارغًا.

**س: أستهدف PDF/A للأرشفة—هل ما زال هذا يعمل؟**  
ج: بالتأكيد. فقط استدعِ `pdfDocument.Convert(new PdfFormatConversionOptions(PdfFormat.PDFA_1_A));` قبل الحفظ. تظل حقول النموذج فعّالة.

## نصائح احترافية وأفضل الممارسات

- **إعادة استخدام أسماء الحقول بحكمة:** إذا كنت تحتاج حقولًا متميزة، أعطِ كلٍ منها `PartialName` فريدًا. إعادة استخدام الاسم نفسه يخلق قيمة مشتركة، وهو ميزة قوية أو مصدر أخطاء إذا نسيت ذلك.  
- **تحويل الإحداثيات:** عند التصميم على الشاشة، قد تعمل بالبيكسل. حوّل إلى نقاط (`points = pixels * 72 / DPI`) لتجنب الأخطاء في الموضع.  
- **نصيحة الأداء:** إذا كنت تولد صفحات كثيرة، أعد استخدام تعريف `TextBoxField` واحد واستنسخه بـ `firstTextBox.Clone()`—هذا يقلل من استهلاك الذاكرة.  
- **التنسيق:** يتيح لك Aspose تضمين الخطوط (`pdfDocument.Fonts.Add(FontRepository.FindFont("Arial"))`) بحيث يبقى المظهر متسقًا عبر المنصات.

## الخطوات التالية

الآن بعد أن عرفت **كيفية إنشاء مستند pdf**، **إضافة صفحة إلى pdf**، **كيفية إضافة مربع نص**، و**حفظ pdf مع نموذج**، يمكنك توسيع الحل:

- إضافة **مربعات اختيار** أو **أزرار راديو** للاستبيانات.  
- ملء النموذج برمجيًا من قاعدة بيانات (مثلاً تعبئة الفواتير).  
- دمج ملفات PDF متعددة في ملف واحد مع الحفاظ على حقول النماذج.  

إذا كنت مهتمًا بإنشاء جداول، صور، أو توقيعات رقمية، اطلع على أدلتنا الأخرى حول *Aspose.PDF for .NET*.

---

**برمجة سعيدة!** لا تتردد في ترك تعليق إذا كان هناك ما غير واضح، أو مشاركة كيفية تخصيصك للنموذج في مشروعك الخاص. 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}