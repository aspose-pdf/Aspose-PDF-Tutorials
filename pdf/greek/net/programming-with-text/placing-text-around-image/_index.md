---
"description": "Μάθετε πώς να τοποθετείτε κείμενο γύρω από εικόνες σε PDF χρησιμοποιώντας το Aspose.PDF για .NET. Ακολουθήστε τον αναλυτικό οδηγό μας για να δημιουργήσετε επαγγελματικά PDF με εικόνες και κείμενο δίπλα-δίπλα."
"linktitle": "Τοποθέτηση κειμένου γύρω από εικόνα σε αρχείο PDF"
"second_title": "Aspose.PDF για αναφορά API .NET"
"title": "Τοποθέτηση κειμένου γύρω από εικόνα σε αρχείο PDF"
"url": "/el/net/programming-with-text/placing-text-around-image/"
"weight": 260
---

{{< blocks/products/pf/main-wrap-class >}}

{{< blocks/products/pf/main-container >}}

{{< blocks/products/pf/tutorial-page-section >}}

# Τοποθέτηση κειμένου γύρω από εικόνα σε αρχείο PDF

## Εισαγωγή

Έχετε δοκιμάσει ποτέ να τοποθετήσετε κείμενο γύρω από μια εικόνα σε ένα αρχείο PDF, αλλά το βρήκατε δύσκολο; Αν ναι, τότε βρίσκεστε στο σωστό μέρος! Το Aspose.PDF για .NET απλοποιεί αυτή τη διαδικασία, επιτρέποντάς σας να τοποθετείτε κείμενο δίπλα σε εικόνες με λίγες μόνο γραμμές κώδικα. Είτε δημιουργείτε αναφορές, έγγραφα είτε παρουσιάσεις, αυτή η λειτουργία είναι ένας φανταστικός τρόπος για να βελτιώσετε τη διάταξη του περιεχομένου σας και να το κάνετε πιο ελκυστικό οπτικά. Σήμερα, θα σας δείξουμε πώς να χρησιμοποιήσετε το Aspose.PDF για .NET για να τοποθετήσετε κείμενο γύρω από εικόνες σε ένα έγγραφο PDF.

## Προαπαιτούμενα

Πριν προχωρήσουμε στον κώδικα, ας βεβαιωθούμε ότι έχουμε ρυθμίσει τα πάντα. Δείτε τι θα χρειαστείτε:

- Aspose.PDF για .NET: Μπορείτε να το κατεβάσετε από [εδώ](https://releases.aspose.com/pdf/net/).
- Visual Studio: Βεβαιωθείτε ότι έχετε εγκαταστήσει την πιο πρόσφατη έκδοση για να συνεχίσετε ομαλά.
- .NET Framework: Αυτό το παράδειγμα χρησιμοποιεί .NET, επομένως βεβαιωθείτε ότι το περιβάλλον σας έχει ρυθμιστεί για ανάπτυξη .NET.
- Προσωρινή Άδεια: Μπορείτε να ζητήσετε προσωρινή άδεια [εδώ](https://purchase.aspose.com/temporary-license/) αν αξιολογείτε το προϊόν.

Εάν δεν έχετε ρυθμίσει ακόμα το Aspose.PDF για .NET, ακολουθήστε τις οδηγίες εγκατάστασης στο [απόδειξη με έγγραφα](https://reference.aspose.com/pdf/net/).

## Εισαγωγή χώρων ονομάτων

Πριν ξεκινήσουμε τον προγραμματισμό, πρέπει να εισαγάγουμε τους απαραίτητους χώρους ονομάτων. Ακολουθεί το απόσπασμα κώδικα για να το κάνουμε αυτό:

```csharp
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
```

Αυτοί οι χώροι ονομάτων είναι απαραίτητοι καθώς παρέχουν πρόσβαση σε κλάσεις όπως `Document`, `Page`, `Image`, και `HtmlFragment`, το οποίο θα χρησιμοποιήσουμε για τη δημιουργία και τον χειρισμό του PDF.

Τώρα που έχουμε θέσει τα πράγματα, ας αναλύσουμε πώς να τοποθετήσετε κείμενο γύρω από μια εικόνα στο αρχείο PDF σας χρησιμοποιώντας το Aspose.PDF για .NET. Θα σας καθοδηγήσουμε βήμα προς βήμα.

## Βήμα 1: Δημιουργία αντικειμένου εγγράφου

Αρχικά, πρέπει να δημιουργήσετε ένα έγγραφο PDF. Στο Aspose.PDF, αυτό γίνεται με τη δημιουργία ενός `Document` αντικείμενο. Αυτό το αντικείμενο θα χρησιμεύσει ως βάση για όλο το περιεχόμενο που θα προσθέσουμε.

```csharp
string dataDir = "YOUR DOCUMENT DIRECTORY";
Aspose.Pdf.Document doc = new Aspose.Pdf.Document();
```

Εδώ, δημιουργήσαμε ένα κενό έγγραφο PDF. Δεν έχει ακόμη σελίδες, αλλά μην ανησυχείτε, θα προσθέσουμε μία στο επόμενο βήμα.

## Βήμα 2: Προσθήκη σελίδας στο έγγραφο

Τώρα που έχουμε το έγγραφό μας, ήρθε η ώρα να προσθέσουμε μια σελίδα. Σκεφτείτε το σαν να δημιουργείτε ένα λευκό φύλλο χαρτιού όπου θα προσθέσετε το περιεχόμενό σας.

```csharp
Aspose.Pdf.Page page = doc.Pages.Add();
```

Αυτός ο κώδικας προσθέτει μια νέα σελίδα στο έγγραφο. Από προεπιλογή, η σελίδα είναι κενή, αλλά πρόκειται να το αλλάξουμε αυτό.

## Βήμα 3: Δημιουργήστε έναν πίνακα για την οργάνωση περιεχομένου

Για να διατηρήσουμε σωστά ευθυγραμμισμένη την εικόνα και το κείμενό μας, θα χρησιμοποιήσουμε έναν πίνακα. Οι πίνακες σε PDF μπορούν να βοηθήσουν στη δομή της διάταξής σας, όπως ακριβώς συμβαίνει σε έγγραφα Word ή HTML.

```csharp
Aspose.Pdf.Table table1 = new Aspose.Pdf.Table();
page.Paragraphs.Add(table1);
```

Αυτό το απόσπασμα δημιουργεί έναν πίνακα και τον προσθέτει στη σελίδα. Σκεφτείτε τον πίνακα ως το πλαίσιο για την ευθυγράμμιση της εικόνας και του κειμένου σας.

## Βήμα 4: Ορισμός πλάτους στηλών για τον πίνακα

Τώρα που προσθέσαμε έναν πίνακα, πρέπει να ορίσουμε το πλάτος των στηλών. Αυτό διασφαλίζει ότι η εικόνα και το κείμενο έχουν το κατάλληλο μέγεθος στη σελίδα.

```csharp
table1.ColumnWidths = "120 270";
```

Αυτή η γραμμή ορίζει το πλάτος δύο στηλών—μίας για την εικόνα και μίας για το κείμενο. Προσαρμόστε αυτές τις τιμές εάν η εικόνα ή το κείμενό σας χρειάζεται περισσότερο ή λιγότερο χώρο.

## Βήμα 5: Ορισμός περιθωρίων και συμπλήρωσης

Για να βεβαιωθούμε ότι όλα φαίνονται τακτοποιημένα, ας προσθέσουμε μερικά περιθώρια και συμπλήρωση στον πίνακα.

```csharp
Aspose.Pdf.MarginInfo margin = new Aspose.Pdf.MarginInfo();
margin.Top = 5f;
margin.Left = 5f;
margin.Right = 5f;
margin.Bottom = 5f;
table1.DefaultCellPadding = margin;
```

Αυτές οι ρυθμίσεις διασφαλίζουν ότι ο πίνακάς σας έχει σταθερή απόσταση, καθιστώντας το περιεχόμενο οπτικά ελκυστικό.

## Βήμα 6: Εισαγωγή εικόνας στον πίνακα

Τώρα, ας περάσουμε στο διασκεδαστικό κομμάτι—την προσθήκη μιας εικόνας. Σε αυτήν την περίπτωση, θα προσθέσουμε το λογότυπο Aspose, αλλά μπορείτε να χρησιμοποιήσετε οποιαδήποτε εικόνα θέλετε.

```csharp
Aspose.Pdf.Row row1 = table1.Rows.Add();
Aspose.Pdf.Image logo = new Aspose.Pdf.Image();
logo.File = dataDir + "aspose-logo.jpg";
logo.FixHeight = 120;
logo.FixWidth = 110;
row1.Cells.Add();
row1.Cells[0].Paragraphs.Add(logo);
```

Να τι συμβαίνει:
- Φορτώνουμε την εικόνα από τον καθορισμένο κατάλογο.
- Ορίζουμε το ύψος και το πλάτος της εικόνας.
- Τέλος, προσθέτουμε την εικόνα στο πρώτο κελί του πίνακα.

## Βήμα 7: Προσθήκη κειμένου δίπλα στην εικόνα

Τώρα που η εικόνα είναι στη θέση της, ας προσθέσουμε κείμενο δίπλα της. Για αυτό το παράδειγμα, θα χρησιμοποιήσουμε κείμενο με μορφοποίηση HTML για να διαμορφώσουμε το περιεχόμενο.

```csharp
string TitleString = "<font face=\"Arial\" size=6 color=\"#101090\"><b> Aspose.Pdf for .NET</b></font>";
string BodyString1 = "<font face=\"Arial\" size=2><br/>Aspose.Pdf for .NET is a non-graphical PDF document reporting component that enables .NET applications to <b>create PDF documents from scratch</b> without utilizing Adobe Acrobat.</font>";

Aspose.Pdf.HtmlFragment TitleText = new Aspose.Pdf.HtmlFragment(TitleString + BodyString1);
row1.Cells.Add();
row1.Cells[1].Paragraphs.Add(TitleText);
```

Αυτό το μπλοκ προσθέτει έναν στυλιζαρισμένο τίτλο και μια περιγραφή στο κελί δίπλα στην εικόνα. Μπορείτε να μορφοποιήσετε το κείμενο χρησιμοποιώντας ετικέτες HTML για περισσότερη προσαρμογή.

## Βήμα 8: Προσαρμογή κάθετης ευθυγράμμισης

Από προεπιλογή, το περιεχόμενο στα κελιά του πίνακα ενδέχεται να μην ευθυγραμμίζεται με τον τρόπο που θέλετε. Σε αυτήν την περίπτωση, θέλουμε να βεβαιωθούμε ότι το κείμενο είναι ευθυγραμμισμένο με το επάνω μέρος του κελιού.

```csharp
row1.Cells[1].VerticalAlignment = Aspose.Pdf.VerticalAlignment.Top;
```

Αυτό διασφαλίζει ότι το κείμενο βρίσκεται στην κορυφή του κελιού, διατηρώντας τη διάταξη καθαρή και επαγγελματική.

## Βήμα 9: Προσθήκη περισσότερου κειμένου κάτω από την εικόνα και την περιγραφή

Ίσως θελήσετε να συμπεριλάβετε περισσότερο περιεχόμενο κάτω από την εικόνα και το κείμενο. Ας προσθέσουμε μια ακόμη γραμμή στον πίνακα για αυτόν τον σκοπό.

```csharp
Aspose.Pdf.Row SecondRow = table1.Rows.Add();
SecondRow.Cells.Add();
SecondRow.Cells[0].ColSpan = 2;
SecondRow.Cells[0].VerticalAlignment = Aspose.Pdf.VerticalAlignment.Top;

string SecondRowString = "<font face=\"Arial\" size=2>Aspose.Pdf for .NET supports the creation of PDF files through API and XML or XSL-FO templates.</font>";
Aspose.Pdf.HtmlFragment SecondRowText = new Aspose.Pdf.HtmlFragment(SecondRowString);
SecondRow.Cells[0].Paragraphs.Add(SecondRowText);
```

Εδώ, προσθέσαμε μια ακόμη γραμμή με επιπλέον κείμενο, που εκτείνεται και στις δύο στήλες για να διατηρήσουμε την ισορροπία στη διάταξη.

## Βήμα 10: Αποθήκευση του εγγράφου PDF

Τέλος, πρέπει να αποθηκεύσουμε το έγγραφο ώστε να μπορείτε να δείτε τις αλλαγές.

```csharp
doc.Save(dataDir + "PlacingTextAroundImage_out.pdf");
```

Αυτό αποθηκεύει το PDF με την εικόνα και το κείμενο μορφοποιημένα ακριβώς όπως τα θέλουμε.

## Σύναψη

Η τοποθέτηση κειμένου γύρω από εικόνες σε ένα PDF μπορεί να φαίνεται δύσκολη, αλλά το Aspose.PDF για .NET απλοποιεί τη διαδικασία. Αξιοποιώντας πίνακες, εικόνες και στυλιζαρισμένο κείμενο, μπορείτε να δημιουργήσετε PDF επαγγελματικής εμφάνισης με ελάχιστη προσπάθεια. Με λίγες μόνο γραμμές κώδικα, μπορείτε να τοποθετήσετε το περιεχόμενο ακριβώς εκεί που θέλετε, δίνοντας στα έγγραφά σας μια κομψή και καλά οργανωμένη εμφάνιση.

## Συχνές ερωτήσεις

### Μπορώ να χρησιμοποιήσω αυτήν τη μέθοδο για να τοποθετήσω πολλές εικόνες με κείμενο;
Ναι, απλώς προσθέστε περισσότερες γραμμές και κελιά στον πίνακά σας για να συμπεριλάβετε επιπλέον εικόνες και κείμενο.

### Μπορώ να αλλάξω την ευθυγράμμιση της εικόνας;
Απολύτως! Μπορείτε να τροποποιήσετε την στοίχιση της εικόνας προσαρμόζοντας τις ιδιότητες στοίχισης του κελιού.

### Πώς μπορώ να διαμορφώσω περαιτέρω το κείμενο;
Μπορείτε να χρησιμοποιήσετε ετικέτες HTML εντός του `HtmlFragment` αντιταχθείτε στην εφαρμογή διαφόρων στυλ όπως έντονη γραφή, πλάγια γραφή ή διαφορετικές γραμματοσειρές.

### Μπορώ να ελέγξω την απόσταση μεταξύ κειμένου και εικόνας;
Ναι, χρησιμοποιώντας το `MarginInfo` Το αντικείμενο σάς επιτρέπει να ελέγχετε την συμπλήρωση και τα περιθώρια μεταξύ των στοιχείων.

### Είναι δυνατή η προσθήκη συνδέσμων στο κείμενο;
Σίγουρα! Μπορείτε να ενσωματώσετε υπερσυνδέσμους στο κείμενο με μορφοποίηση HTML χρησιμοποιώντας το `<a>` ετικέτα.

{{< /blocks/products/pf/tutorial-page-section >}}

{{< /blocks/products/pf/main-container >}}

{{< /blocks/products/pf/main-wrap-class >}}

{{< blocks/products/products-backtop-button >}}